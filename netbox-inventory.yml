defaults:
    sample: 'api-sample.json'
    api_url: 'http://localhost/api/dcim/devices/'

groupBy:
    default:
        - device_role
        - rack
        - platform
    custom:
        - env

hostsVars:
    custom:
        - env
